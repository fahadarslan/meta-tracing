<?xml version="1.0" encoding="UTF-8"?>
<manifest>
  <!-- This is the default manifest for this branch.  It's sort of a hodge-podge -->
  <!-- of repositories used for various development.  Look to specific named -->
  <!-- manifest files for focussed development and builds -->

  <!-- Derive from the MEL 2014.12 release configuration as a base -->
  <include name="mel_2014.12.xml"/>

  <!-- Updates for post-release -->
  <project name="mel-updates.git" path="updates"/>

  <project name="meta-amd.git" path="meta-amd"/>
</manifest>
